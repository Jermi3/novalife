import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "NovaLife Wellness | Transform Your Life",
  description: "Expert fitness coaching, personalized nutrition programs, and longevity-focused wellness solutions. Start your transformation journey with NovaLife today.",
  keywords: "fitness coaching, nutrition, longevity, wellness, personal training, biohacking",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}

